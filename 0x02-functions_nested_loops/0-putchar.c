#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - prints "_putchar"
 *
 * Return: 0 (success)
 */
int main(void)
{
	char x = '_', p = 'p', u = 'u', t = 't';
	char c = 'c', h = 'h', a = 'a', r = 'r', y = '\n';

	putchar(x);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);
	putchar(y);

	return (0);
}
