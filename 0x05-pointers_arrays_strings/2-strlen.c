#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: the string character
 * Return: len
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);
}
